version: 2

sources:
  - name: bronze
    schema: bronze
    tables:
      - name: drivers
        columns:
          - name: driver_id
            tests:
              - not_null
      - name: constructors
        columns:
          - name: constructor_id
            tests:
              - not_null
      - name: races
        columns:
          - name: season
            tests:
              - not_null
          - name: round
            tests:
              - not_null
      - name: race_result
        columns:
          - name: season
            tests:
              - not_null
          - name: round
            tests:
              - not_null